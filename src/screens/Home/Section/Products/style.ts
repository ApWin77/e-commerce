import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    paddingTop: 5,
  },
  headTitle: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#000',
    marginHorizontal: 16,
  },
});
